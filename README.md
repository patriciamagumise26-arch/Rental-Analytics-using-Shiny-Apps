# Rental-Analytics-using-Shiny-Apps
 built this Shiny application to analyze U.S. apartment listing data by state, city, and time. The interactive filters and dynamic visualizations allow users to explore housing price trends and gain insights through real-time data exploration

# Run 
pip install -r requirements.txt
python -m shiny run project_app.py
